// @ts-check
import { defineConfig } from 'astro/config';

// Static output (default). Compression happens in the browser via canvas.
export default defineConfig({
  site: 'https://lilroi.netlify.app',
});
